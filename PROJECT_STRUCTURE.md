# Netscanner 项目结构和文件说明

## 📂 完整目录结构

```
Netscanner/
├── src/                              # 源代码主目录
│   ├── __init__.py                  # Python包初始化文件
│   ├── scanner.py                   # 核心扫描引擎模块
│   ├── cli.py                       # 命令行界面模块
│   ├── gui.py                       # 图形用户界面模块
│   ├── utils.py                     # 工具函数模块
│   └── __pycache__/                 # Python缓存目录（自动生成）
│
├── docs/                             # 文档目录
│   └── 使用手册.md                  # 详细的用户使用手册
│
├── build/                            # PyInstaller构建输出目录
│   └── 网络主机扫描器/              # 打包中间文件
│
├── dist/                             # 发行版本输出目录
│   └── 网络主机扫描器.exe           # 最终可执行文件
│
├── main.py                           # 程序主入口文件
├── build.py                          # PyInstaller打包脚本
├── pyinstaller_config.py            # PyInstaller配置文件（可选）
├── requirements.txt                  # Python依赖声明文件
├── pyproject.toml                   # Python项目元数据配置
├── README.md                         # 项目说明文档
├── LICENSE.txt                       # MIT许可证
├── release.bat                       # Windows打包脚本
├── release.sh                        # Linux/macOS打包脚本
├── run_tests.py                      # 测试运行脚本
└── 网络主机扫描器.spec              # PyInstaller规范文件

```

## 📄 各主要文件详细说明

### 1. **main.py** - 程序主入口
**功能**：程序的总入口，负责启动图形界面或命令行界面
**主要内容**：
- `resource_path()` - 获取资源文件路径（支持PyInstaller打包）
- `check_dependencies()` - 检查依赖包是否已安装
- `run_gui()` - 启动图形界面模块
- `run_cli()` - 启动命令行界面模块
- `show_about()` - 显示关于信息
- 菜单系统让用户选择运行模式

**关键特点**：
- 支持PyInstaller打包后的资源访问
- 完整的依赖检查机制
- 错误处理和异常捕获

**使用方式**：
```bash
python main.py          # 默认启动菜单
python main.py gui      # 启动图形界面
python main.py cli      # 启动命令行界面
```

---

### 2. **src/scanner.py** - 核心扫描引擎
**功能**：网络主机扫描的核心模块，处理所有扫描逻辑
**主要类和方法**：

#### `HostInfo` 数据类
```python
@dataclass
class HostInfo:
    ip: str              # 主机IP地址
    status: str          # 主机状态：'在线' 或 '离线'
    mac: str             # MAC地址
    hostname: str        # 主机名
    response_time: float # 响应时间（毫秒）
    os_type: str         # 操作系统类型
```

#### `NetworkScanner` 主类
**核心方法**：
- `__init__(max_threads, timeout)` - 初始化扫描器
- `scan_range(target, progress_callback)` - 扫描IP范围/网段/单IP
- `ping_host(ip)` - 对单个主机进行Ping探测
- `get_mac_address(ip)` - 获取主机MAC地址
- `get_hostname(ip)` - 获取主机名
- `export_results(results, format, filepath)` - 导出扫描结果
- `parse_target(target)` - 解析扫描目标格式

**支持的目标格式**：
- 单个IP：`192.168.1.1`
- IP范围：`192.168.1.1-100` 或 `192.168.1.1-192.168.1.100`
- CIDR网段：`192.168.1.0/24`

**导出格式支持**：
- CSV - 逗号分隔的表格格式
- JSON - JSON格式数据
- TXT - 纯文本格式
- Excel - Excel电子表格（需pandas和openpyxl）

**关键特点**：
- 多线程并发扫描（默认100线程）
- 跨平台支持（Windows、Linux、macOS）
- 自适应超时设置
- 实时进度回调

---

### 3. **src/cli.py** - 命令行界面模块
**功能**：提供命令行接口供用户在终端中使用
**主要内容**：
- `main()` - CLI主函数
- 基于argparse的参数解析
- 实时扫描进度显示
- 结果统计和导出

**支持的参数**：
```bash
python main.py cli [目标] [选项]

参数说明：
  目标                扫描目标 (必需)
  -t, --threads      线程数 (默认: 100)
  -o, --output       输出文件路径 (可选)
  --format           输出格式: csv|json|txt|excel (默认: txt)
  --timeout          超时时间(秒) (默认: 2)
```

**使用示例**：
```bash
# 扫描单个IP
python main.py cli 192.168.1.100

# 扫描IP范围，使用50个线程，3秒超时
python main.py cli 192.168.1.1-100 -t 50 --timeout 3

# 扫描网段并导出CSV
python main.py cli 192.168.1.0/24 -o results.csv --format csv

# 扫描并导出Excel
python main.py cli 192.168.1.0/24 -o results.xlsx --format excel
```

**关键特点**：
- 清晰的扫描进度条
- 详细的统计信息展示
- 支持Ctrl+C中断
- 完整的错误处理

---

### 4. **src/gui.py** - 图形用户界面模块
**功能**：提供完整的图形界面供用户使用
**主要类**：

#### `ScannerGUI` 类
**核心方法**：
- `__init__()` - 初始化GUI窗口和控件
- `setup_ui()` - 设置用户界面布局
- `start_scan()` - 启动扫描操作
- `stop_scan()` - 停止当前扫描
- `quick_scan()` - 快速扫描当前网络
- `export_results()` - 导出扫描结果
- `check_queue()` - 定期检查扫描结果队列

**界面组件**：
- **扫描设置区**
  - 目标输入框
  - 线程数设置（默认100）
  - 超时时间设置（默认2秒）
  - 开始/停止按钮
  - 快速扫描按钮

- **结果显示区**
  - 实时扫描进度条
  - 在线主机数统计
  - 详细的结果表格
  - 扫描日志展示

- **导出功能**
  - 多格式导出（CSV/JSON/TXT/Excel）
  - 文件保存对话框

**关键特点**：
- 多线程扫描（不卡UI）
- 实时进度显示
- 美观的Tkinter界面
- 完整的交互体验

---

### 5. **src/utils.py** - 工具函数模块
**功能**：提供各种辅助和工具函数
**主要内容**：
- IP地址验证和解析
- 日期时间格式化
- 日志记录函数
- 网络相关工具函数
- 文件操作辅助函数

---

### 6. **requirements.txt** - 依赖声明文件
**内容**：
```
# 核心依赖
pandas>=1.3.0           # 数据处理和Excel导出
openpyxl>=3.0.0         # Excel文件操作

# 开发依赖
black>=22.0.0           # 代码格式化工具
flake8>=4.0.0           # 代码检查工具
pytest>=7.0.0           # 单元测试框架

# 打包依赖（可选）
pyinstaller>=5.0.0      # 打包工具
```

**安装所有依赖**：
```bash
pip install -r requirements.txt
```

---

### 7. **pyproject.toml** - 项目元数据配置
**功能**：Python项目的标准配置文件
**主要内容**：
- 项目基本信息（名称、版本、作者）
- 依赖声明
- 构建系统配置
- 开发工具配置

---

### 8. **build.py** - 打包脚本
**功能**：使用PyInstaller将Python程序打包成可执行文件
**主要功能**：
- `build_windows()` - Windows平台打包
- `build_linux()` - Linux平台打包
- `build_macos()` - macOS平台打包
- 自动清理之前的构建文件
- 嵌入所有必要的资源和依赖

**使用方式**：
```bash
python build.py
```

**生成的可执行文件**：
- Windows：`dist/网络主机扫描器.exe`
- Linux/macOS：`dist/网络主机扫描器`

---

### 9. **release.bat** 和 **release.sh** - 发布脚本
**功能**：快速发布脚本，集成了环境检查和打包过程

**Windows (release.bat)**：
```bash
release.bat
```

**Linux/macOS (release.sh)**：
```bash
chmod +x release.sh
./release.sh
```

---

### 10. **README.md** - 项目说明文档
**内容**：
- 项目简介和功能
- 系统要求
- 快速开始指南
- 详细使用示例
- 参数说明
- 常见问题解答
- 技术支持信息

---

### 11. **docs/使用手册.md** - 详细使用手册
**内容**：
- 软件安装步骤
- 快速开始教程
- 详细功能说明
- 参数配置指南
- 常见问题和解决方案
- 故障排除指南
- 技术支持联系方式

---

### 12. **LICENSE.txt** - MIT许可证
**说明**：开源许可证，允许自由使用、修改和分发

---

## 🔄 程序执行流程

```
启动 main.py
    ↓
检查依赖包
    ├→ 缺失 → 显示错误信息 → 退出
    └→ 正常 → 继续
    ↓
显示菜单 (如果没有命令行参数)
    ├→ 选择 GUI 模式
    │    ↓
    │  加载 gui.py → ScannerGUI 类
    │    ↓
    │  创建 Tkinter 窗口
    │    ↓
    │  用户输入目标 → 点击"开始扫描"
    │    ↓
    │  调用 scanner.py 进行扫描
    │    ↓
    │  实时显示进度和结果
    │    ↓
    │  导出结果 (可选)
    │
    └→ 选择 CLI 模式
         ↓
      加载 cli.py → main() 函数
         ↓
      解析命令行参数
         ↓
      调用 scanner.py 进行扫描
         ↓
      显示进度条和统计信息
         ↓
      导出结果 (可选)
```

---

## 🔧 模块依赖关系

```
main.py (主入口)
    ├─→ src/gui.py (GUI模块)
    │       └─→ src/scanner.py (核心扫描引擎)
    │           ├─→ src/utils.py (工具函数)
    │           └─→ tkinter, pandas, openpyxl等
    │
    └─→ src/cli.py (CLI模块)
            └─→ src/scanner.py (核心扫描引擎)
                ├─→ src/utils.py (工具函数)
                └─→ pandas, openpyxl等
```

---

## 📊 类和对象关系图

```
HostInfo (数据类)
    ├─ ip: str
    ├─ status: str
    ├─ mac: str
    ├─ hostname: str
    ├─ response_time: float
    └─ os_type: str

NetworkScanner (核心扫描器)
    ├─ scan_range(target) → List[HostInfo]
    ├─ ping_host(ip) → HostInfo
    ├─ get_mac_address(ip) → str
    ├─ get_hostname(ip) → str
    ├─ export_results(results, format, filepath)
    └─ parse_target(target) → List[str]

ScannerGUI (图形界面)
    ├─ scanner: NetworkScanner (实例)
    ├─ root: tk.Tk (Tkinter窗口)
    ├─ start_scan()
    ├─ stop_scan()
    ├─ export_results()
    └─ check_queue()
```

---

## 🚀 打包和部署

### 构建可执行文件

**Windows**：
```bash
python build.py
# 或
python release.bat
```

**Linux/macOS**：
```bash
chmod +x release.sh
./release.sh
```

### 发布清单
- ✅ 源代码（src/）
- ✅ 文档（docs/）
- ✅ 配置文件（requirements.txt, pyproject.toml）
- ✅ 打包脚本（build.py, release.bat, release.sh）
- ✅ 许可证（LICENSE.txt）
- ✅ 说明文档（README.md）

---

## 🔒 代码特性

1. **跨平台兼容**：支持Windows、Linux、macOS
2. **多线程设计**：高效的并发扫描能力
3. **错误处理**：完整的异常捕获机制
4. **用户友好**：GUI和CLI双界面
5. **灵活导出**：支持多种输出格式
6. **易于打包**：集成PyInstaller配置
7. **代码规范**：遵循PEP 8标准

---

## 📝 开发和维护

### 代码风格
使用Black进行代码格式化：
```bash
black src/
```

### 代码检查
使用Flake8进行代码审查：
```bash
flake8 src/
```

### 单元测试
使用Pytest运行测试：
```bash
python run_tests.py
# 或
pytest
```

---

## 📞 技术支持和联系

- **GitHub**: https://github.com/Ara602/Netscanner
- **作者**: 张夏灵
- **邮箱**: 3518023245@qq.com
- **班级**: 网云2302
- **学号**: 542307280233

